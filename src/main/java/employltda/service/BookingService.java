package employltda.service;
import org.springframework.beans.factory.annotation.Autowired;
import javassist.tools.rmi.ObjectNotFoundException;
import java.util.*;

//public class BookingService<BookingRepository, Booking> {

    //@Autowired
    //private BookingRepository repo;

    //public Booking search(Integer id){
       // Booking obj = repo.findOne(id);
       // if(obj == null){
          //  throw new ObjectNotFoundException("Booking id not found:" + id);
       // }
       // return obj;
   // }

   // public Booking insert (Booking obj){
       // obj.setId(null);
       // return repo.save(obj);
   // }


//}